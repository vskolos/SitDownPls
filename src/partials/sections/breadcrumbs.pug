.breadcrumbs.container
  ul.lst.breadcrumbs__list
    each text, index in breadcrumbLinks
      li.breadcrumbs__item
        a(class=`lnk breadcrumbs__link${index === breadcrumbLinks.length - 1 ? ' breadcrumbs__link--current' : ''}` href='#')= text
